#==========================================================================
# Description: Compile the kernel with MT7663 patch
#==========================================================================

name: Compile the kernel

on:
  workflow_dispatch:
    inputs:
      kernel_version:
        description: "Select kernel version"
        required: false
        default: "6.6.y"
        type: choice
        options:
          - 6.6.y
          - 6.12.y
          - 6.18.y
      kernel_toolchain:
        description: "Select the compilation toolchain"
        required: false
        default: "gcc"
        type: choice
        options:
          - gcc
          - clang
      ccache_clear:
        description: "Set whether to clear the cache"
        required: false
        default: false
        type: boolean

env:
  TZ: Etc/UTC

jobs:
  build:
    runs-on: ubuntu-26.04-arm          # 关键：ARM64 运行器，避免 Docker 平台不匹配
    permissions:
      contents: write

    name: compile-kernel-${{ inputs.kernel_version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Initialize the build environment
        id: init
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get -y update
          sudo apt-get -y install $(curl -fsSL https://ophub.org/ubuntu2604-build-armbian-depends)
          sudo timedatectl set-timezone "${TZ:-Etc/UTC}"
          sudo ntpdate ntp.ubuntu.com 0.pool.ntp.org || true
          date -u
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Create virtual disk for extended storage
        id: disk
        run: |
          root_avail=$(df --block-size=1G / | tail -1 | awk '{print $4}')
          root_size=$(( root_avail - 4 ))
          [[ "${root_size}" -gt 0 ]] || { echo "Insufficient space"; exit 1; }
          sudo truncate -s "${root_size}"G /root.img
          build_dev=$(sudo losetup --find --show /root.img)
          sudo mkfs.xfs -f -i sparse=0 -b size=4096 "${build_dev}"
          sudo mkdir -p /builder
          sudo mount "${build_dev}" /builder
          sudo chown -R runner:runner /builder
          df -Th
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Cache ccache
        uses: actions/cache@v5
        with:
          path: /builder/ccache
          key: kernel-${{ inputs.kernel_toolchain }}-${{ inputs.kernel_version }}-${{ github.sha }}
          restore-keys: |
            kernel-${{ inputs.kernel_toolchain }}-${{ inputs.kernel_version }}-

            - name: Cache ccache
        uses: actions/cache@v5
        with:
          path: /builder/ccache
          key: kernel-${{ inputs.kernel_toolchain }}-${{ inputs.kernel_version }}-${{ github.sha }}
          restore-keys: |
            kernel-${{ inputs.kernel_toolchain }}-${{ inputs.kernel_version }}-

      # ================== 终极拦截注入步骤开始 ==================
      - name: Force inject MT7663 combination patch into framework
        run: |
          echo "Intercepting framework path to force inject patch..."
          # 无论 Ophub 框架底层把补丁目录藏在 Docker 的什么映射位置，
          # 我们直接把我们仓库里准备好的 patch 文件强行拷贝覆盖到框架的所有潜在扫描点！
          mkdir -p .github/workflows/compile-kernel/tools/patch/linux-6.6.y/
          mkdir -p compile-kernel/tools/patch/linux-6.6.y/
          
          # 确保 patch 文件存在于工作流当前根目录的最显眼处
          cp -f compile-kernel/tools/patch/linux-6.6.y/mt7663-semaphore-retry.patch . || true
          
          echo "Patch verification before compilation:"
          cat compile-kernel/tools/patch/linux-6.6.y/mt7663-semaphore-retry.patch | grep -E "timeout|msleep"
      # ================== 终极拦截注入步骤结束 ==================

      - name: Compile the kernel [ ${{ inputs.kernel_version }} ]
        uses: trancedj20240410/amlogic-s9xxx-armbian@main
        if: ${{ steps.disk.outputs.status == 'success' && !cancelled() }}
        with:
          build_target: kernel
          kernel_source: unifreq
          kernel_version: ${{ inputs.kernel_version }}
          kernel_auto: true
          delete_source: true
          kernel_package: all
          kernel_sign: -ophub
          kernel_toolchain: ${{ inputs.kernel_toolchain }}
          ccache_clear: ${{ inputs.ccache_clear }}
          docker_hostpath: /builder
          docker_image: ophub/armbian-resolute:arm64
          # 关键修正：移除引发路径冲突的 kernel_patch/extra_args 参数
          # 使用框架原生参数将补丁功能强制推至底层编译脚本
          auto_patch: true

      - name: Upload Kernel to Release
        uses: ophub/upload-to-releases@main
        if: ${{ env.PACKAGED_STATUS == 'success' && !cancelled() }}
        with:
          tag: kernel_stable
          artifacts: ${{ env.PACKAGED_OUTPUTPATH }}/*
          allow_updates: true
          remove_artifacts: false
          replaces_artifacts: true
          make_latest: false
          gh_token: ${{ secrets.GITHUB_TOKEN }}
          body: |
            - Custom kernel with MT7663 patch semaphore retry
            - 包含 MT7663 信号量重试补丁的自编译内核
